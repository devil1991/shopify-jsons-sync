name: 'Shopify Theme JSON Syncs'
description: 'Sync Shopify Theme JSONs'
author: 'devil1991'
inputs:
  store:
    required: true
    description: 'Store'

  theme:
    required: true
    description: 'Target Theme ID'
runs:
  main: 'dist/index.js'
  using: 'node16'
